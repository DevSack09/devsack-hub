// Mutaciones de Tool (crear, editar, eliminar, destacar, cambiar estado).
// Se implementan en la siguiente iteración como Server Actions ("use server");
// cada una debe validar la sesión con auth() antes de tocar Prisma.
export {};
